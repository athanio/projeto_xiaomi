# Minimal production requirements for the Django backend
Django==5.2.5
django-cors-headers==4.9.0
django-ninja==1.6.2
djangorestframework==3.17.1
python-decouple==3.8
python-dotenv==1.0.0
httpx==0.28.1
gunicorn==26.0.0
whitenoise==6.12.0

# Add only what you need; avoid platform-specific packages here.
